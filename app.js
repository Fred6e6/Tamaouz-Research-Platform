const form=document.getElementById('requestForm');const panels=[...document.querySelectorAll('.step-panel')];const fill=document.querySelector('.progress-fill');const label=document.getElementById('stepLabel');const review=document.getElementById('review');let current=0;function showStep(n){current=n;panels.forEach((p,i)=>p.classList.toggle('active',i===n));fill.style.width=((n+1)/panels.length*100)+'%';label.textContent=`Step ${n+1} of ${panels.length}`;if(n===3)buildReview();document.getElementById('request').scrollIntoView({behavior:'smooth',block:'start'})}function englishOnly(e){const value=e.target.value;if(/[\u0600-\u06FF]/.test(value)){e.target.setCustomValidity('Please enter information in English only.');e.target.reportValidity()}else e.target.setCustomValidity('')}document.querySelectorAll('.english').forEach(i=>i.addEventListener('input',englishOnly));function validatePanel(){const fields=[...panels[current].querySelectorAll('input,select,textarea')];for(const el of fields){if(el.type==='radio')continue;if(!el.checkValidity()){el.reportValidity();return false}}if(current===0&&!document.querySelector('input[name="requestType"]:checked')){alert('Please select a request type.');return false}return true}document.querySelectorAll('.next').forEach(b=>b.addEventListener('click',()=>{if(validatePanel()&&current<panels.length-1)showStep(current+1)}));document.querySelectorAll('.back').forEach(b=>b.addEventListener('click',()=>{if(current>0)showStep(current-1)}));document.querySelectorAll('.service-card').forEach(card=>card.addEventListener('click',()=>{const radio=[...document.querySelectorAll('input[name="requestType"]')].find(r=>r.value===card.dataset.type);if(radio){radio.checked=true;showStep(0);setTimeout(()=>showStep(1),250)}else location.hash='request'}));function buildReview(){const data=new FormData(form);const type=data.get('requestType')||'—';review.innerHTML=`<div><b>Request Type</b><span>${escapeHtml(type)}</span></div><div><b>Full Name</b><span>${escapeHtml(data.get('fullName')||'—')}</span></div><div><b>University / Workplace</b><span>${escapeHtml(data.get('workplace')||'—')}</span></div><div><b>Project Title</b><span>${escapeHtml(data.get('projectTitle')||'—')}</span></div><div><b>Deadline</b><span>${escapeHtml(data.get('deadline')||'—')}</span></div>`}function escapeHtml(s){return String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}form.addEventListener('submit',e=>{e.preventDefault();if(!validatePanel())return;const id='TM-'+new Date().getFullYear()+'-'+String(Math.floor(10000+Math.random()*90000));document.getElementById('requestId').textContent=id;form.hidden=true;document.querySelector('.request-intro').hidden=true;document.getElementById('success').hidden=false;window.scrollTo({top:document.getElementById('request').offsetTop-80,behavior:'smooth'})});document.getElementById('files').addEventListener('change',e=>{const box=e.target.closest('.upload');const files=[...e.target.files];box.querySelector('p').textContent=files.length?files.map(f=>f.name).join(', '):'Templates, instructions, previous work or other relevant files.'});