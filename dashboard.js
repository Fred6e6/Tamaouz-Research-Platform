const KEY='tamaouz_requests';
const statuses=['Submitted','Under Review','In Progress','Ready for Review','Completed'];
function getRequests(){try{return JSON.parse(localStorage.getItem(KEY)||'[]')}catch{return[]}}
function saveRequests(items){localStorage.setItem(KEY,JSON.stringify(items))}
function esc(s){return String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function card(r,admin=false){return `<article class="request-card"><div class="request-head"><div><div class="request-id">${esc(r.id)}</div><p>${esc(r.type)}</p></div>${admin?`<select class="status-select" data-id="${esc(r.id)}">${statuses.map(s=>`<option ${s===r.status?'selected':''}>${s}</option>`).join('')}</select>`:`<span class="badge">${esc(r.status)}</span>`}</div><div class="meta"><div><small>Student</small><strong>${esc(r.fullName)}</strong></div><div><small>University / Workplace</small><strong>${esc(r.workplace)}</strong></div><div><small>Deadline</small><strong>${esc(r.deadline||'Not specified')}</strong></div><div><small>Submitted</small><strong>${esc(r.createdAt)}</strong></div></div></article>`}
function renderStudent(){const list=document.getElementById('requestList'),empty=document.getElementById('empty');if(!list)return;const items=getRequests();empty.hidden=items.length>0;list.innerHTML=items.map(r=>card(r)).join('')}
function renderAdmin(){const list=document.getElementById('adminList');if(!list)return;const q=(document.getElementById('search')?.value||'').toLowerCase(),filter=document.getElementById('statusFilter')?.value||'';const items=getRequests().filter(r=>(!filter||r.status===filter)&&(!q||`${r.id} ${r.fullName} ${r.type}`.toLowerCase().includes(q)));list.innerHTML=items.length?items.map(r=>card(r,true)).join(''):'<div class="empty"><h2>No matching requests</h2><p>Try changing the search or status filter.</p></div>';list.querySelectorAll('.status-select').forEach(s=>s.addEventListener('change',e=>{const all=getRequests();const r=all.find(x=>x.id===e.target.dataset.id);if(r){r.status=e.target.value;saveRequests(all);renderAdmin()}}))}
if(document.getElementById('requestList'))renderStudent();
if(document.getElementById('adminList')){document.getElementById('search').addEventListener('input',renderAdmin);document.getElementById('statusFilter').addEventListener('change',renderAdmin);renderAdmin()}
